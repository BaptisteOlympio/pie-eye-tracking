syntax = "proto3";

service OpenFaceService {
    rpc GetLandmark (Frame) returns (Landmark) {}
}

message Frame {
    bytes frame = 1;
    bytes dtype = 2;
    bytes shape = 3;
}

message Landmark {
    bytes landmark = 1;
}